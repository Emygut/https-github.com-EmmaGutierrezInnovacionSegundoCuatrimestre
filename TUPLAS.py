# Ejercicios Tuplas: 
# Crear tres tuplas con datos random. 
# Crear una lista que las contenga y mostrarla.
import random

# Tupla de nombres
nombres = ("Juan", "María", "Carlos", "Ana", "Luis")
print (nombres)
# Tupla de coordenadas
coordenadas = ((0, 0), (3, 5), (-2, 8), (10, -1))
print (coordenadas)
# Tupla de colores
colores = ("rojo", "verde", "azul", "amarillo", "morado")
print (colores)
# Crear la lista que contiene las tuplas
lista_de_tuplas = [nombres, coordenadas, colores]
print (lista_de_tuplas)
